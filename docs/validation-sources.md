# Where the validation rules come from

Generated by `node engine/build-sources.mjs` — do not edit by hand.

Re-Playable makes **54 checks** against a playable. It fixes 23 of them automatically and hands
the rest back with an explanation of what a person has to decide.

Every check names the source it rests on. Nothing in this document is typed by hand: it is generated
from the same table the tool checks against, and `node engine/test.mjs` fails if a check is added
without declaring a source.

There are two kinds of checking, and they rest on different things.

| | What it is | What powers it |
|---|---|---|
| **1 · Rule-based** | Reads the file and decides from the code. Same file, same answer, every time. No network, no model. | 3 families of published specification, listed below |
| **2 · AI policy scan** | Optional, off until you paste your own API key. Looks at the artwork the way a reviewer would. | Google's advertising policies, captured verbatim in this repository |

The readiness score comes from part 1 only. The AI scan is advisory and never changes it.

---

## 1 · Rule-based — the tool detects and fixes from the code

### Google's own documentation

What Google requires of an App campaign HTML5 asset, and what its policies forbid.

- **[Google Ads policies](https://support.google.com/adspolicy/answer/6008942)** — captured in this repository as `docs/google-ads-policies.md`, so the wording can be checked without leaving it.
- **[About HTML5/Playable ads for App campaigns](https://support.google.com/google-ads/answer/9981650)**
- **[Fix issues with HTML5 assets for App campaigns](https://support.google.com/google-ads/answer/12771973)**

31 checks rest on these.

| Check | Source | Severity | Handling | What the source states |
|---|---|---|---|---|
| `ad-orientation` | HTML5 spec | Warning | **Fixed** | Orientation is declared with `<meta name="ad.orientation">`; absent, the asset renders portrait-only with no error. |
| `ad-orientation-bad` | HTML5 spec | Warning | **Fixed** | An unparseable value falls back to portrait-only, silently. |
| `audio-ungated` | HTML5 spec | Advice | **Fixed** | Same rule, plus: all sound stops on the exit click. |
| `autoplay-media` | HTML5 spec | Warning | **Fixed** | Sound must not be enabled before the user interacts with the ad. |
| `close-control` | Ads policies | Advice | Reported | Misrepresentation. Non-functional controls are named on the Misleading ad design detail page, which is not captured. |
| `copy-in-images` | Ads policies | Warning | Reported | Says the copy checks below could not read this creative — every word is drawn into artwork — so their silence proves nothing and only the AI scan can see it. |
| `custom-exit` | HTML5 issues | Blocker | **Fixed** | Custom exits are not allowed in App campaign HTML5 (CUSTOM_EXIT_NOT_ALLOWED). |
| `custom-exit-fallback` | HTML5 issues | Warning | **Fixed** | Same rule; downgraded because a Google exit is already present, so the branch is likely unreachable. |
| `custom-exit-link` | HTML5 issues | Warning | **Fixed** | An outbound link is a custom exit and loses the click. |
| `data-collection` | Ads policies | Blocker | Reported | Data collection and use: advertising partners should not collect user information for unclear purposes or without disclosure. |
| `dialogs` | Ads policies | Warning | **Fixed** | Misrepresentation covers ads that deceive users; system-style dialogs read as imitating device UI. The detail page is not captured. |
| `entry-not-root` | HTML5 spec | Warning | **Fixed** where possible | Google looks for the entry HTML at the root of the archive. |
| `exitapi-dynamic` | HTML5 issues | Warning | Reported | Same consequence: the entire ad becomes the click target. |
| `exitapi-placement` | HTML5 issues | Warning | **Fixed** | Injected by other JavaScript instead of a literal tag, the whole ad area becomes clickable at serve time. |
| `exitapi-script` | HTML5 spec | Blocker | **Fixed** | exitapi.js loads from tpc.googlesyndication.com as a literal `<script>` in `<head>`. |
| `external-refs` | HTML5 issues | Blocker | Reported | Fetching from outside the package is a fourth-party call and is disapproved. |
| `filenames` | HTML5 issues | Blocker | **Fixed** | File and folder names may use only letters, numbers, dot, dash and underscore. |
| `missing-local-ref` | HTML5 spec | Blocker | Reported | A playable must be self-contained; paths not in the package fail in the ad slot. |
| `no-cta-copy` | Ads policies | Advice | Reported | Editorial: ads must be clear and easy to interact with. |
| `no-google-exit` | HTML5 spec | Blocker | **Fixed** where possible | The click-through is registered by calling ExitApi.exit(); nothing else counts. |
| `prohibited-amp` | HTML5 issues | Blocker | Reported | AMP tags and cdn.ampproject.org are prohibited. |
| `prohibited-enabler` | HTML5 issues | Blocker | **Fixed** where possible | DoubleClick Studio Enabler.js is prohibited. |
| `prohibited-iframe` | HTML5 issues | Blocker | Reported | Frames of any kind are prohibited. |
| `prohibited-producttype` | HTML5 issues | Blocker | **Fixed** | `<meta name="productType" content="dynamic">` is prohibited. |
| `prohibited-storage` | HTML5 issues | Blocker | **Fixed** | Browser storage is prohibited in HTML5 ad assets. |
| `prohibited-video-nosrc` | HTML5 issues | Blocker | Reported | A `<video>` with no src attribute is disallowed. |
| `script-syntax` | Ads policies | Blocker | Reported | Technical requirements name "HTML5 ads that don’t function properly or appear blank". |
| `suspect-nav` | HTML5 issues | Warning | Reported | Same rule, through a reference the tool cannot identify, so it is reported rather than rewritten. |
| `zip-files` | HTML5 spec | Blocker | Reported | No more than 512 files inside the .ZIP. |
| `zip-headroom` | HTML5 spec | Advice | Reported | Same 5 MB cap; this warns before an edit crosses it. |
| `zip-size` | HTML5 spec | Blocker | **Fixed** where possible | App campaign playables upload as a .ZIP of at most 5 MB. |

### Other ad networks' playable specifications

What the file was built against. These say what has to be replaced, not what Google wants.

- **[IAB MRAID specification](https://www.iab.com/guidelines/mobile-rich-media-ad-interface-definitions-mraid/)**
- **[ironSource / Unity LevelPlay playable (DAPI)](https://developers.is.com/ironsource-mobile/general/playable-ads/)**
- **[Meta playable ads specification](https://developers.facebook.com/docs/app-ads/formats/playable-ads)**
- **[Mintegral playable specification](https://www.mintegral.com/en/advertiser/playable)**
- **[TikTok / Pangle playable specification](https://ads.tiktok.com/help/article/playable-ads)**
- **[Vungle / Liftoff playable specification](https://support.vungle.com/hc/en-us/articles/360048622551)**

9 checks rest on these.

| Check | Source | Severity | Handling | What the source states |
|---|---|---|---|---|
| `sdk-also` | MRAID, DAPI, Meta | Warning | **Fixed** | Same, softened because the file already carries a Google exit. |
| `sdk-also-mintegral` | Mintegral | Advice | Reported | Same, downgraded to advice: the file already calls ExitApi.exit(), so its Google path should win and this branch never run. |
| `sdk-also-tiktok` | TikTok/Pangle | Advice | Reported | Same, downgraded to advice: the file already calls ExitApi.exit(), so its Google path should win and this branch never run. |
| `sdk-also-vungle` | Vungle | Advice | Reported | Same, downgraded to advice: the file already calls ExitApi.exit(), so its Google path should win and this branch never run. |
| `sdk-calls` | MRAID, DAPI, Meta | Blocker | **Fixed** | A creative waits for the SDK’s ready signal before starting, so with no SDK it never boots. |
| `sdk-script` | MRAID, DAPI, Meta | Blocker | **Fixed** | The host injects mraid.js at serve time; on Google it does not exist and the request fails. |
| `sdk-unsupported-mintegral` | Mintegral | Warning | Reported | A network whose call surface the tool has no stand-in for. Reported for a person to check in the preview rather than rewritten. |
| `sdk-unsupported-tiktok` | TikTok/Pangle | Warning | Reported | A network whose call surface the tool has no stand-in for. Reported for a person to check in the preview rather than rewritten. |
| `sdk-unsupported-vungle` | Vungle | Warning | Reported | A network whose call surface the tool has no stand-in for. Reported for a person to check in the preview rather than rewritten. |

### Web standards

How a browser behaves when the document is incomplete. Not an ad rule — a rendering one.

- **[Browser behaviour (HTML and JavaScript standards)](https://developer.mozilla.org/)**

4 checks rest on this.

| Check | Source | Severity | Handling | What the source states |
|---|---|---|---|---|
| `charset` | Web standards | Warning | **Fixed** | Undeclared encoding renders non-ASCII copy as mojibake. |
| `doctype` | Web standards | Warning | **Fixed** | Without a doctype the browser uses quirks mode, changing the layout maths a game relies on. |
| `truncated` | Web standards | Blocker | Reported | A document with no closing `</html>` is incomplete and will not run. |
| `viewport` | Web standards | Warning | **Fixed** | Without a viewport a mobile browser assumes desktop width and scales the ad down. |

### The copy patterns

The captured policy page also drives 10 pattern checks that read the ad's words and flag the phrasing
behind the most common disapprovals. These are still rule-based — plain patterns over the text, no
model — but their source is the policy page, which is why the same document appears in both halves
of this list.

Where the captured page states the rule, the finding quotes Google's sentence, shown below. Where it
does not, the finding says **unsourced** rather than putting words in Google's mouth: 7 of 10 are
quoted. The captured page is Google's overview; several sub-policies live on detail pages that were
not captured, which is what the remaining 3 rest on.

| Check | Source | Severity | Handling | What the source states |
|---|---|---|---|---|
| `copy-adult-terms` | Ads policies | Warning | Reported | Google Ads restricts certain kinds of sexual content in ads and destinations, which will only show in limited scenarios based on user search queries, user age and local laws where the ad is being served. |
| `copy-cash-promise` | Ads policies | Warning | Reported | making misleading or unrealistic claims regarding weight loss or financial gain |
| `copy-fake-system-ui` | Ads policies | Warning | Reported | Ads may not mimic system warnings, dialogs, notifications, or device UI. "Update available", "Your phone…", "Virus detected" style copy is a standard disapproval. |
| `copy-gambling-terms` | Ads policies | Advice | Reported | Gambling-related ads are allowed if they comply with the policies below and the advertiser has received the proper Google Ads certification. |
| `copy-gimmick-copy` | Ads policies | Advice | Reported | overly generic ads that contain vague phrases such as "Buy products here" |
| `copy-restricted-vertical` | Ads policies | Warning | Reported | The policies below cover content that is sometimes legally or culturally sensitive. |
| `copy-shouting-punctuation` | Ads policies | Warning | Reported | gimmicky use of words, numbers, letters, punctuation, or symbols such as FREE, f-r-e-e, and F₹€€!! |
| `copy-store-badge-copy` | Ads policies | Advice | Reported | Store names and badges are fine only as the official, unmodified assets. Hand-drawn "Google Play" or "App Store" buttons get flagged. |
| `copy-superlative-claims` | Ads policies | Advice | Reported | making offers that aren't actually available |
| `copy-urgency-bait` | Ads policies | Warning | Reported | Copy that pressures the user with false scarcity or deadlines is treated as clickbait. |

---

## 2 · AI policy scan — the optional review

**Source:** [Google Ads policies - Advertising Policies Help](https://support.google.com/adspolicy/answer/6008942?hl=en)
**Captured:** 2026-09-10 · **In this repository:** `docs/google-ads-policies.md` — 21 KB of Google's own text

The scan exists for one reason: **a playable usually draws its words instead of writing them.** A
canvas game renders every headline and button into an image, so the pattern checks above have
nothing to read. An AI that can see the artwork is the only pass that reaches them.

How it is kept honest:

1. The captured policy page goes into the prompt verbatim, generated by `engine/build-rubric.mjs`. Nothing is retyped or summarised.
2. The model is told to judge against that text alone, and to quote the sentence it relies on.
3. Every quote is checked against the captured text before it is shown. A paraphrased or invented one is marked **unsourced**, and Google's wording is not put on screen.
4. The result is advisory. It never moves the readiness score.

Works with Gemini, Claude, OpenAI, Kimi, Qwen, GLM, or any OpenAI-compatible endpoint. The key goes
from the browser to the service you chose, and is stored only in that browser.

---

## How each check is grounded

| Basis | Checks | What it means |
|---|---|---|
| **captured** | 12 | The source text is in this repository. The finding quotes it, and the quote is checked against the file. |
| **documented** | 29 | Stated on the public page cited. The page is linked; its text is not copied here. |
| **vendor** | 9 | The other ad network's own playable specification, not Google's. |
| **measured** | 1 | Established by running real playables, not taken from a document. |
| **unsourced** | 3 | Not stated on the captured page. Shown as unsourced rather than presented as Google's words. |

## What these sources do not settle

They say what Google requires. They do not say what Google will approve — that is decided by
Google's own validator and its policy reviewers, on the day, on the file you upload.

Re-Playable's job is to remove the failures that are knowable from the file itself, so that what
reaches a reviewer is the creative and not a packaging mistake. Before uploading for real, put the
converted `.zip` through Google's free
[HTML5 validator](https://h5validator.appspot.com/adwords/asset): it is instant, and it settles the
technical half with certainty.
